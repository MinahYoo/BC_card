# 제1회 AI금융빅데이터플랫폼 소비데이터 공모전 — 프로젝트 계획

## 주제
**"소비는 있는데 왜 못 버티는가 — 설명 가능한 상권 생존 모델"**

BC카드 소비데이터로 상권의 수요 구조를 읽고, 그 상권의 실제 생존율(개폐업)이 왜 갈리는지 통계적으로 설명 가능하게 분해하는 모델.

BC카드 기존 서비스(상권지수 — 스냅샷 등급, Biz Credit — 개별 가맹점 휴폐업 예측)와 다르게, **"지역×업종 단위의 생존 구조를 원인별로 분해"**하는 진단 레이어를 채우는 포지셔닝.

---

## 데이터 구성 (역할 고정)

| 데이터 | 성격 | 역할 |
|---|---|---|
| BC카드 소비데이터 (기본제공, 242,574행) | 시군구×업종×성별×연령×월 집계 (2026.1~6월) | 수요측 공변량 — 지역×업종별 연령·성별 소비구성 |
| 지방행정 인허가데이터 LOCALDATA (data.go.kr, 무료) | 개별 사업장 단위, 인허가일자·폐업일자·영업상태 | 생존분석 관측단위 |
| 소진공 상가(상권)정보 (data.go.kr, 무료) | 현재 영업 중인 전국 상가업소, 업종·경위도 | 공급측 공변량 — 업종 밀집도, 공간구조 |

**주의**: BC카드 데이터는 개별 사업장 식별자가 없는 집계치이므로 생존분석의 관측단위가 될 수 없음. 생존분석은 LOCALDATA에서만, BC카드는 그 생존모델의 설명변수로만 사용.

---

## 분석 파이프라인

### 0단계 — 안전판
전체(11업종×255시군구) 확장 전에 표본 큰 업종 1개(예: 편의점)로 조인→전처리→생존모델 전체 흐름을 먼저 끝까지 검증.

### 1단계 — 데이터 정합
- LOCALDATA 업종 ↔ BC카드 11개 업종 매핑 (업종분류 연계표 활용)
- LOCALDATA 세부주소(도로명) → 시군구 단위로 집계
- BC카드 결측 조합은 "0"이 아니라 "비공개(소규모셀 마스킹)"로 처리
- 상호명+주소+업종 매칭 시 신뢰도 낮은 케이스 별도 플래그

### 2단계 — Kaplan-Meier (1차 스크리닝)
지역×업종별 생존곡선 산출 → 어디서 어떤 업종이 유독 빨리 죽는지 1차 발견. 이 패턴이 3단계 모델링 가설이 됨.

### 3단계 — Cox PH / Random Survival Forest
- 종속변수: 생존시간 + censoring(폐업여부)
- 공변량: BC카드 소비구성(연령·성별 비중), 소진공 동일업종 경쟁밀도, (여유 시) 인접 시군구 공간 lag
- 소규모 지역 표본 불안정 대응: 부분 풀링(partial pooling / empirical Bayes)으로 표본 적은 지역 추정치 수축
- Cox(해석 쉬움, hazard ratio) vs RSF(성능↑, SHAP 필요) — 진행 시간 보고 확정

### 4단계 — XAI 분해 (독창성 핵심)
생존율/hazard 격차를 만드는 요인을 SHAP으로 분해 → "이 상권 이 업종이 위험한 이유는 경쟁밀도 X%, 연령구성 불일치 Y%..." 형태로 원인 제시.

### 5단계 (옵션, 여유 있을 때만) — 공간 확장
인접상권 그래프 spatial lag 또는 GNN. 0~4단계 완성 후 진행.

---

## 기획 파트 (실현가능성 20%, 비중 작게)
결과를 "지역×업종 생존 리스크 리포트" 웹페이지로 시각화. 타겟은 **예비창업자 또는 BC카드 가맹점전략팀 중 하나로 좁혀서 명시** (둘 다 언급 시 산만해 보임 — 확정 필요).

---

## 차별화 근거 (독창성 40% 방어 논리)
- BC카드 상권지수·Biz Credit = 스냅샷 / 예측(prediction)
- 이 모델 = 왜 갈리는지 구조적 원인 분해(explanation), 방법론도 생존분석+XAI로 상이

---

## 한계 정리

### 구조적 한계 (기법으로 해결 불가 — 명시만)
- **폐업≠실패**: 이전·업종전환·단순휴업이 "폐업"으로 뭉뚱그려질 수 있음 → "생존"을 "해당 사업장·해당 업종으로서의 지속"으로 정의 범위를 좁혀 방어
- **BC카드 데이터의 시군구 집계 단위**: 원본이 이미 시군구 레벨이라 더 세밀한 동단위 소비패턴 복원 불가

### 부분적으로 완화 가능 (실제 적용)
- **소규모 지역 표본 불안정** → 최소 표본수 threshold + 부분 풀링(empirical Bayes)으로 극단치 수축
- **LOCALDATA 사업자번호 부재로 인한 매칭 모호성** → 상호명+주소+업종 매칭, 신뢰도 낮은 케이스 플래그 처리 및 비중 투명 보고
- **주소단위 집계 손실** → 생존분석(개폐업 이벤트) 자체는 LOCALDATA 원 주소단위 유지, BC카드 공변량만 시군구 레벨로 조인해 손실 범위를 공변량 파트로 국한

---

## 미결정 사항 (팀 확정 필요)
- [ ] 마감일
- [ ] 팀 인원 수 및 역할 분담 (생존모델링+XAI / 데이터 조인·전처리 / 시각화·웹페이지)
- [ ] Cox PH vs RSF 중 메인 모델 확정
- [ ] 5단계(공간확장/GNN) 진행 여부
- [ ] 기획 파트 타겟 오디언스 (예비창업자 vs BC카드 가맹점전략팀) 확정
